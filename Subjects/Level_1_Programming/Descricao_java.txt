
public class ContaBancaria { // Declarando a classe Contabancaria
2 - public String titular; // Declarando atributo texto classe bancaria
public String senha; // Declarando atributo texto classe bancaria
public double saldo; // Declarando atributo numero com casas decimais classe bancaria

3 – public void deposito(double valor) { // Criando uma função deposito
saldo = saldo + valor; //Guardando a conta de valor dentro do 
System.out.println("novo saldo " + saldo);
}
public void saque(double valor) {
if (saldo >= valor) {
System.out.println("retirada " + valor);
saldo = saldo - valor;
}
else
System.out.println("saldo insuficiente para a retirada de " + valor);
}
 4 – public String extratoSimples() {
return "correntista " + titular + " valor atual da conta " + saldo;
}
}
______________________________________________________________________
importjava.util.Scanner;
public class ProgramaBanco {
5 - public static void main(String args[]) {
6 - Scanner entradaDados = new Scanner( System.in );
7 – ContaBancaria contaUm = new ContaBancaria();
System.out.print("Digite o nome do titular da conta ");Laura
8 - contaUm.titular = entradaDados.nextLine();
System.out.print("Digite o saldo inicial "); 200
contaUm.saldo = entradaDados.nextDouble();
System.out.print("Digite o valor do saque ");
9 - contaUm.saque(entradaDados.nextDouble());
System.out.println(contaUm.extratoSimples());
System.out.print("Digite o valor do deposito ");
contaUm.deposito(entradaDados.nextDouble());
10 - System.out.println(contaUm.extratoSimples());
}
}
Obs: não inserir os números das linhas no momento da programação